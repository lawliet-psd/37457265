name: Kablo Kontrol

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */1 * * *'  # Her 2 saatte bir çalışır

jobs:
  KabloGuncelle:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: write

    steps:
      - name: Depoyu Klonla
        uses: actions/checkout@v4.2.2

      - name: Python 3.11.8 Yükle
        uses: actions/setup-python@v5.3.0
        with:
          python-version: 3.11.8

      - name: Gereksinimleri Kur
        run: |
          python -m pip install --upgrade pip
          pip install -U setuptools wheel Kekik httpx parsel requests

      - name: API'den M3U Listesini Al
        run: python Pythonlar/kablo.py  

      - name: kbltv klasörünü güncelle
        run: |
          set -e
          mkdir -p kbltv
          copy_file() {
            local src="$1"
            local dest="$2"
            if [ -f "$src" ]; then
              cp -f "$src" "$dest"
              echo "Kopyalandı: $src -> $dest"
            elif [ -f "Pythonlar/$src" ]; then
              cp -f "Pythonlar/$src" "$dest"
              echo "Kopyalandı: Pythonlar/$src -> $dest"
            else
              echo "Uyarı: Kaynak bulunamadı: $src"
            fi
          }
          copy_file "kablo_çocuk_eğitim.m3u" "kbltv/cocuk.m3u"
          copy_file "kablo_ulusal.m3u" "kbltv/ulusal.m3u"
          copy_file "kablo_ulusal_haber.m3u" "kbltv/haber.m3u"
          copy_file "kablo_spor.m3u" "kbltv/spor.m3u"
          copy_file "kablo_belgesel.m3u" "kbltv/belgesel.m3u"
          copy_file "kablo_sinema_ve_dizi.m3u" "kbltv/sinema.m3u"

      - name: Değişiklik Kontrolü
        id: degisiklik_kontrol
        run: |
          git diff --quiet kbltv || echo "degisiklik=true" >> $GITHUB_OUTPUT

      - name: Değişiklikleri Commit Et
        if: steps.degisiklik_kontrol.outputs.degisiklik == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add kbltv/cocuk.m3u kbltv/ulusal.m3u kbltv/haber.m3u kbltv/spor.m3u kbltv/belgesel.m3u kbltv/sinema.m3u
          git commit -m "📦 kbltv M3U güncellemesi" -m "🕒 Otomatik işlem - $GITHUB_SHA"
          git push
